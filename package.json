{
  "name": "opencode-hexz",
  "version": "1.2.2",
  "description": "HEXZ — OpenCode upgrade layer. Anti-slop, security scanning, design scaffolds, web search, image handling, plugin marketplace.",
  "type": "module",
  "main": "dist/hexz.js",
  "exports": {
    ".": "./dist/hexz.js"
  },
  "scripts": {
    "build": "bun build src/hexz.ts --outfile dist/hexz.js --target node --external @opencode-ai/plugin --external bun",
    "typecheck": "tsc --noEmit",
    "dev": "bun run build --watch",
    "prepublishOnly": "bun run build"
  },
  "files": [
    "dist",
    "README.md",
    "CONTRIBUTING.md"
  ],
  "keywords": [
    "opencode",
    "opencode-plugin",
    "hexz",
    "ai",
    "security",
    "design",
    "anti-slop"
  ],
  "peerDependencies": {
    "@opencode-ai/plugin": "*",
    "typescript": "^5 || ^6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@opencode-ai/plugin": "^1.17.7"
  }
}
